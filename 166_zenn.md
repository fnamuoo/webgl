# Babylon.js ：UpDownなコースを作る

## この記事のスナップショット

![](https://static.zenn.studio/user-upload/93d57778ae0f-20260911.gif)
*コースの表裏：青＝表、赤＝裏（6倍速）*

![](https://static.zenn.studio/user-upload/0ceed6cf6a11-20260911.gif)
*ランダムなコース（3倍速）*

https://playground.babylonjs.com/?BabylonToolkit#UX3THG

（上記のURLにおいて、ツールバーの歯車マークから「EDITOR」のチェックを外せばウィンドウいっぱいに、歯車マークから「FULLSCREEN」を選べば画面いっぱいになります。）

## ソース

https://github.com/fnamuoo/webgl/blob/main/166

:::message
ローカルで動かす場合、上記ソースに加え、別途 git 内の [136/js](https://github.com/fnamuoo/webgl/tree/main/136/js) を ./js として配置してください。
:::

## 概要

[three.js](https://threejs.org/) のデモにある [https://phoboslab.org/wipeout/](https://phoboslab.org/wipeout/) や 公式 [Building a Track for a Carriage to Follow](https://doc.babylonjs.com/guidedLearning/workshop/Track/) の デモ [Roller Coaster Overview](https://playground.babylonjs.com/#SQFG0Q#5) に触発されて、似たものを作れないかと挑戦し、「UpDownなコース」を作りました。

Babylon.jsのPath3Dを基礎に、3D空間を自由にアップダウンするランダムなコースを生成し、
姿勢制御・周回化・非交差化・表裏走行まで実現する方法を試しました。

最終的に目指すものは、下記を満たすコースをつくることにあります。

- 3D空間を上下する
- ランダム生成できる
- 周回できる
- できるだけ交差しない
- バンクをつけない／コース上を移動できるように

## やったこと

- パスを作る（経路そのものの生成）
  - ランダムで周回コースをつくる
  - コースを交差させないために（幾何ハッシュで交差確認）
  - 交差させないコース（没案）
- パスからメッシュを作る（バンクなしの実現）
  - コースメッシュのつくりかた
  - バンクさせないコースの作り方
  - 【コラム】両面を使ったメビウスコース（脱線）
- コースのトレース（移動体・カメラで追跡）

### パスを作る（経路そのものの生成）

3D空間上でアップダウンなコースを作るには、ランダムに配置した点群を結んで点列とすれば良い話ですが、より自然なコースにするために次の制限を設けます。
始点から方向性をもたせて次の点を配置していき、更にランダムで方向を変化させます。
左右は大きめに上下は小さめに変化させることで自然な変化な感じになります。
Babylon.js 的には点列をスプライン補間して Path3Dとして扱います。

#### ランダムで周回コースをつくる

ランダムウォークでコースを作ると周回コースにはなりません。
ランダムで且つ周回コースを作る方法は、検索したり生成AIに問い合わせると幾つか方法が見つかります。ここでは簡単に「残り○％で始点に向かう」アルゴリズムを利用します。ここで末端から始点に直接つなぐように点を配置すると急カーブができてしまいます。滑らかな角度でつながるようにするため、末端からの接線方向がもう片方の端点に向かうように目標地点を設けます。この目標地点を通過するようにランダムウォークの進行方向を方向づけさせることで、滑らかにつなぎます。

![](https://static.zenn.studio/user-upload/fc02a00100db-20260911.jpg)
*始点（左側）と末端（右側）との目標地点（緑球）と経路*

#### コースを交差させないために（幾何ハッシュで交差確認）

ランダムウォークでコースを作るとコースが交差する場合があります。
交差させないようなコースを作る方法は、検索したり生成AIに問い合わせると幾つか方法が見つかります。ここでは過去に実装した「幾何ハッシュ」を用いて交差判定を行います。幾何ハッシュに、Path3Dの経路を登録しつつ延長させていき、延長した先に既に経路があるようなら向きを変更します。
おおむね上手く機能しているようですが、設定が甘かったのか稀に交差することがあります。

![](https://static.zenn.studio/user-upload/7a20c42e756d-20260911.gif)
*交差しそうでしていない経路*

※ 幾何ハッシュの補足説明

幾何ハッシュは事前に（3D座標、ID）を登録しておいて、指定座標の近傍のIDを取得する方法で
下記の記事でも使っており、このときは指定座標値近くのメッシュ（ドミノ）を取得するために使っています。 [Babylon.js で物理演算(havok)：ドミノ倒しで一筆書き](https://zenn.dev/fnamuoo/articles/a395ed2578d0d4)

#### 交差させないコース（没案）

交差させないコースをつくる別案として、グリッド上の点を選ぶ方法があります。
生成AIにアルゴリズムを任せたせいか、上下優先で選択しているようでコースっぽくありません。
また、まがるときは 90度回転するので変化が目まぐるしくて、見てるだけでもとても疲れます。
結果、ボツとしました。

![](https://static.zenn.studio/user-upload/4bb68a8c5e97-20260911.jpg)
*グリッドをベースとしたコース*

空間を埋めるようにコースを作るより、無駄に空間を使いながらコース作りするのが緩急ができてよさそうです。

### パスからメッシュを作る（バンクなしの実現）

#### コースメッシュのつくりかた

コースには三次元空間内の点列を結んで補間（滑らかに）した点列を使います。
点列は疑似コード（～ブロックのように直線や曲線のメタ情報の組合せ）で作成します。

点列にそった曲線(Path3D)を利用してコースをつくるには tube, extrude が考えられますがここでは ribbon を使います。ribbon は経路を作るためのメッシュではありませんが、[Babylon.js：ribbonで作る列車／車メッシュ](https://zenn.dev/fnamuoo/articles/b29bcfeeb7bf72) の時のように、１枚の平面を板金加工するかのようにして経路を作ります。

手法   | 利点 | 欠点
-------|------|-------
tube   | 経路のみで簡単にメッシュ作成可                     | 断面形状が円形（正多角形）に制限される<br>部分的なロールが発生する
extrude| 経路と断面情報が必要だが、より柔軟なメッシュ作成可 | 部分的なロールが発生する
ribbon | 任意座標に合わせた柔軟なメッシュ作成可             | 矩形グリッドを変形して作れる形状であること<br>全座標の指定が必要

ribbon を使った例には下記の記事があります。
このときコーナーでバンクを意図的に付けていましたが、少々不自然さがあるので今回は使わないことにします。 [Babylon.js ：凹凸のある道路をキャラクターコントローラーでCNSと競争する](https://zenn.dev/fnamuoo/articles/6625ebaa3feb26) 
tube や extrude を使うとどうしてもバンクが自動的に発生してしまいます。下記は意図的に（手動で明示的に）バンクをつけたものですが、ここではバンクを付けないことが目的となります。自動的に、バンクしないように逆向きにバンクさせることが難しいそうなので、座標計算のしやすい ribbon を使います（計算手順については次の節で）。

- [Babylon.js で物理演算(havok)：コースに部分的バンクをつけて試走](https://zenn.dev/fnamuoo/articles/2bdc098520d0b8)
- [Babylon.js で物理演算(havok)：ダウンヒルにバンクをつける](https://zenn.dev/fnamuoo/articles/7a24928baa4d55)

#### バンクさせないコースの作り方

経路（Path3D）が垂直方向に移動しないことを利用して、法線方向と従法線方向を作り直し、バンクさせないコースの点群を作ります。
Path3Dの方向の接線方向（進行方向）と垂直方向から、Path3D上の任意の点で従法線(binormal)が水平面上にあるように定め、法線方向を確定します。従法線が決まれば道の両端の座標値が決まり、法線方向から道の両端が決まります。
道の両端の点連をつなげて ribbon のデータとしてメッシュを作成します。

![](https://static.zenn.studio/user-upload/6a06d80eb9d9-20260911.jpg)
*バンクさせないコース*


#### 【コラム】両面を使ったメビウスコース（脱線）

Path3Dの始点と終点の接線方向が同じになるようにして、座標値をつなぐと周回コースが作れますが、コースのねじれはどうしても発生します。通常は、このねじれに応じた逆向きの回転を施すことで連続したコースにすることができます。
ちょっと脱線して、ここで180度回転させてつなぐと「メビウスの輪」のように表裏を使ったコースを作ることができます。なお後述の「コースのトレース」でメッシュを動かすときは、表裏に合わせて法線方向を正順・逆順にして座標計算を行います（簡単に計算するなら周回の偶数・奇数に応じて法線方向の向きを入れ替えます）。

![](https://static.zenn.studio/user-upload/f1f257e510a5-20260911.jpg)
*両面を使ったコース*


### コースのトレース（移動体・カメラで追跡）

コースを作ったからには、コース上の視点で見たくなります。
メッシュは簡単に四角として、コース上を走らせ、カメラで追跡させます。
事前にコース上の点とそれらの点に対する座標(_plist)、接線方向(_vtlist)および法線方向(_vnlist)を計算しておき、これらを利用して表示位置や姿勢（クォータニオン）を設定します。

```js
// レンダリング設定
scene.onBeforeRenderObservable.add((scene) => {
    mesh._i = mesh._i2;
    mesh._i2 = (mesh._i+1) % mesh._n;
    mesh.position.copyFrom(mesh._plist[mesh._i].add(mesh._vnlist[mesh._i].scale(mesh._size_)));
    let vF = mesh._vtlist[mesh._i];
    let vU = mesh._vnlist[mesh._i];
    // 方向ベクトルからクォータニオンを作成
    mesh.rotationQuaternion = BABYLON.Quaternion.FromLookDirectionLH(vF, vU);
});
```

![](https://static.zenn.studio/user-upload/5fdbd6360387-20260911.gif)
*コース上目線（3倍速）*

## まとめ・雑感

物理演算まで辿り着けず、メッシュの動作止まりでした。
次回の物理演算（物理シミュレーション）に応用した例を示します。

